<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!-- /storage/emulated/0/Download/com.bugly.upgrade.demo/.beta/apk-->
    <external-path
        name="beta_external_path"
        path="Download/" />

    <!--/storage/emulated/0/Android/data/com.bugly.upgrade.demo/files/apk/-->
    <external-path
        name="beta_external_files_path"
        path="Android/data/" />

    <!-- This is the path to save the photo image, which must be configured -->
    <external-path
        name="images"
        path="Pictures" />
    
    <!-- Temporary access to all file permissions under the root directory -->
    <external-path
        name="external_files"
        path="." />

</paths>